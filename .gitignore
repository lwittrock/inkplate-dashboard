# Credentials — NEVER commit
config.h
secrets.h

# Generated at build time by CI (or by a future local build script).
# Local builds use the fallback in Dashboard.ino if this file is missing.
firmware_version.h

# Arduino build artifacts
build/
*.elf
*.bin
*.hex
*.map

# OS files
.DS_Store
Thumbs.db

# Claude Code local overrides
.claude/settings.local.json

# Personal working notes (free-form scratch file at repo root)
TODO
