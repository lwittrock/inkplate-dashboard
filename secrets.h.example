#ifndef SECRETS_H
#define SECRETS_H

// Copy to secrets.h and fill in your values. secrets.h is gitignored
// AND deny-listed in .claude/settings.local.json — never commit, never
// read into AI context.

const char* WIFI_SSID     = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
const char* NS_API_KEY    = "YOUR_NS_API_KEY";

#endif
